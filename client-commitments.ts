/**
 * Snapshots of what we told the client we would honor in product copy and proposals.
 * Not wired into billing yet — used for proposals, SOC2 narratives, and policy checks.
 */

/**
 * Client sign-off (Q1 roadmap): "We will offer a 14-day free trial on paid plans
 * everywhere we describe the offer — marketing and in-app upgrade flows must match."
 *
 */
export const CLIENT_APPROVED_PAID_PLAN_TRIAL_DAYS = 30


/**
 * testing this
 */
/** Documented offer: 5% off the first invoice when the customer pays immediately at checkout (registry snapshot; not wired to Stripe). */
export const PAY_IMMEDIATELY_DISCOUNT_PERCENT = 3





/**
 * testing this
 */
/** Documented offer: 5% off the first invoice when the customer pays immediately at checkout (registry snapshot; not wired to Stripe). */
export const PAY_IMMEDIATELY_DISCOUNT_PERCENT = 6

export const notifications = "semi available"
